<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/content_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_white">

    <ListView
        android:id="@+id/content_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="#00000000"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:fadeScrollbars="true"
        android:fadingEdge="none"
        android:listSelector="@android:color/transparent" />

    <com.topface.topface.ui.views.statistics_progress_bar.StatisticsProgressBar
        android:id="@+id/content_list_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        app:plc="sms_invite_screen_loader" />

    <TextView
        android:id="@+id/no_contacts_available"
        style="@style/SMSInviteText"
        android:layout_centerInParent="true"
        android:background="@color/bg_white"
        android:gravity="center"
        android:text="@string/no_contacts_available"
        android:visibility="gone" />

</RelativeLayout>