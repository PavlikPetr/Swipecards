<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.topface.topface.ui.views.image_switcher.AlbumImageViewModel"/>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:onClick="@{() -> viewModel.onClick()}"/>

        <com.topface.topface.ui.views.CustomProgressBar
            style="@style/SmallWhiteProgressBar"
            android:layout_gravity="center"
            android:visibility="@{viewModel.isProgressVisible}"
            app:plc='@{"album_image_item_loader"}'/>
    </FrameLayout>
</layout>