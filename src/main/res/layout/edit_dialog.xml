<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="268dp"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/edit_dialog_limit"
        android:textColor="@color/text_color_gray_transparent"
        android:textSize="18sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        tools:text='1024/1024'
        android:paddingTop="20dp"
        android:paddingBottom="12dp"
        android:paddingLeft="1dp"
        android:paddingRight="1dp" />

    <TextView
        android:id="@+id/edit_dialog_title"
        style="@style/SelectorDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@id/edit_dialog_limit"
        android:maxLines="2"
        android:ellipsize="end"
        tools:text='Very long long text to fill all that gap and overlay on limit text'/>

    <View
        android:id="@+id/edit_dialog_title_divider"
        android:layout_width="fill_parent"
        android:layout_height="2dp"
        android:background="@color/tab_counter_text_color_selected"
        android:layout_below="@id/edit_dialog_title"/>

    <ListView
        android:id="@+id/edit_dialog_options_list"
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:divider="@color/gray_divider"
        android:dividerHeight="1dp"
        android:layout_below="@id/edit_dialog_title_divider"
        android:scrollbars="none"
        android:maxLines="2"
        android:ellipsize="end"
        android:descendantFocusability="afterDescendants" />

    <ViewStub
        android:id="@+id/edit_dialog_buttons_stub"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_dialog_options_list" />
</RelativeLayout>
